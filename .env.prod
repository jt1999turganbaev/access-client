# Frontend backend bilan bitta domenda (nginx) turadi deb hisoblanadi.
# Boshqa domen bo'lsa: VITE_API_URL=http://host/api/tablet va backendda CORS yoqilishi kerak.
VITE_API_URL=/api/tablet
VITE_BACKEND_ORIGIN=
